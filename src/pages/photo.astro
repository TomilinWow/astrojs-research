
---
import axios from "axios";
import Layout from '../layouts/Layout.astro'
import { IPhoto } from "../types/photos";
import PhotoCard from "../components/PhotoCard";
// const { photos } = await axios.get<IPhoto[]>('https://jsonplaceholder.typicode.com/photos');
// const { photos } = Astro.props
// const response = await fetch('https://jsonplaceholder.typicode.com/photos')
// const photos = (await response.json()) as IPhoto[]
const response = await fetch('https://jsonplaceholder.typicode.com/photos')
const products = (await response.json());
console.log('products', products)
---

<Layout title="photo">
    <div class="grid grid-cols-2 gap-4 ml-2 mr-2">
        {products?.map((photo) => {
            return <PhotoCard client:load {...photo} key={photo.id} />
        })}
    </div>
</Layout>
